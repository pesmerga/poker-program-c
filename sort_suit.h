// Library header file: sort_suit.h#ifndef sortsuit#include <iostream.h>#include <iomanip.h>#include <stdlib.h>#include "apstring.h"#include "apmatrix.h"#include "structures.h"// Function sort// Purpose: Sort any hand according to big to small value// Input: hand myhand// Output: hand myhand that has been sorted for a big card and small cardhand sort(hand &myhand);// Function suited// Purpose: Check if the two cards in a players hand are sorted// Input: hand myhand// Output: hand myhand with a changed value of suited if it is suitedhand suited(hand &myhand);#define sortsuit#endif