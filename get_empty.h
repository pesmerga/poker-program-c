// Library Header File: get_empty.h#ifndef getempty#include "structures.h"// Function get_empty// Purpose: determine who is empty and, if they are, give that seat the specific empty input// Input: a table of players(hands)// Output: a table of player(hands) now newly updated with the empty players being modifiedtable get_empty(table &mytable);#define getempty#endif