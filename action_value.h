// Library header file: action_value.h#ifndef actionvalue#include <iostream.h>#include <iomanip.h>#include <stdlib.h>#include "apstring.h"#include "apmatrix.h"#include "ranking.h"#include "structures.h"#include "get_action.h"#include "get_empty.h"// Function action_value// Purpose: change the variation of the inserted fixed percentages given by the situation// Input: a table, 2 fixed values of "bet" and "call"// Output: a table with the new percentages for the computer actiontable action_value(table &mytable,					float &bet,					float &call,					int &rank,					float &delta_bet_loose,					float &delta_bet_passive,					float &delta_bet_rank,					float &delta_call_loose,					float &delta_call_passive,					float &delta_call_rank);					#define actionvalue#endif