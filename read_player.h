// Library header file: read_player.h#ifndef readplayer#include "structures.h"#include <math.h>// Function read_player// Purpose: predict what hands can be discounted that the players on the table still in // after flop would hold knowing the # of times they betted and the # of times they called// in their certain position before the flop// Input: the hand of the currentplayer, the hand that might be discounted, and a table// Output: an integer dispalying if it was a certain case and, if not, the integer 0.float read_player(hand &dummy_hand, table &mytable, int &dummy_player);#define readplayer#endif